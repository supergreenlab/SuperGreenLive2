{
  "name": "SuperGreenLivePI2",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "NODE_ENV=dev NODE_OPTIONS=--openssl-legacy-provider nuxt",
    "build": "NODE_OPTIONS=--openssl-legacy-provider nuxt build",
    "start": "NODE_OPTIONS=--openssl-legacy-provider nuxt start",
    "generate": "NODE_OPTIONS=--openssl-legacy-provider nuxt generate"
  },
  "dependencies": {
    "@nuxtjs/axios": "^5.13.1",
    "core-js": "^3.9.1",
    "luxon": "^1.26.0",
    "nuxt": "^2.15.3",
    "nuxt-client-init-module": "^0.3.0",
    "stylus": "^0.54.8",
    "stylus-loader": "^4.3.3",
    "vue-select": "^3.13.2"
  }
}
